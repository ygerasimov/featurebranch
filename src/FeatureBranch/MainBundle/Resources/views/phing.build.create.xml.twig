<project default="git_clone">

  <target name="git_clone">
    <property name="repo.dir" value="{{ apache_root }}/{{ branch }}" />

    <exec
      command="git clone --branch {{ branch }} {{ repo_origin }} {{ apache_root }}/{{ branch }}"
      />

  </target>
</project>